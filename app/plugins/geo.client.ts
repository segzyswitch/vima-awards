// export default defineNuxtPlugin(async () => {
//   const locale = useLocaleStore()

//   if (!locale.countryCode) {
//     const geo = await $fetch('/api/geo') // your server endpoint
//     locale.setFromGeo(geo)
//   }
// })
